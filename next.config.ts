import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* images.unsplash.com */
  images: {
    domains: ["images.unsplash.com", "placekitten.com", "plus.unsplash.com"],
  },
};

export default nextConfig;
